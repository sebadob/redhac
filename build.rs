// Note: needs a c++ compiler to work:
// dnf install -y gcc-c++

fn main() {
    // tonic_build::configure()
    //     .build_client(true)
    //     .build_server(true)
    //     .out_dir("src/rpc/")
    //     .compile(&["proto/cache.proto"], &["proto"])
    //     .expect("Failed to compile proto/cache.proto");
}
